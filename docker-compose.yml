services:
  uploader:
    build: .
    container_name: uploader
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - TZ=Asia/Tokyo
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
